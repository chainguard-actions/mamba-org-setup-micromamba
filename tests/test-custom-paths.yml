name: Test micromamba-root-path and micromamba-binary-path inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          micromamba-root-path: /tmp/custom-micromamba-root
          micromamba-binary-path: /tmp/custom-micromamba-bin/micromamba
          init-shell: none
          generate-run-shell: "false"
          post-cleanup: all
      - name: Assert micromamba binary exists at custom path
        run: |
          if [ ! -f "/tmp/custom-micromamba-bin/micromamba" ]; then
            echo "FAIL: micromamba binary not found at custom path"
            exit 1
          fi
          echo "PASS: micromamba binary found at custom path"
      - name: Assert micromamba root exists at custom path
        run: |
          if [ ! -d "/tmp/custom-micromamba-root" ]; then
            echo "FAIL: micromamba root not found at custom path"
            exit 1
          fi
          echo "PASS: micromamba root found at custom path"
